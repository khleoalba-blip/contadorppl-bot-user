name: Build and Release APK

on:
  push:
    branches:
      - master
      - main
    paths:
      - 'flutter_dashboard/**'
      - '.github/workflows/build.yml'
  workflow_dispatch:

jobs:
  build:
    name: Build and Release APK
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: flutter_dashboard

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.24.x'

      - name: Cache Flutter dependencies
        uses: actions/cache@v4
        with:
          path: /opt/hostedtoolcache/flutter
          key: flutter-${{ runner.os }}-${{ hashFiles('flutter_dashboard/pubspec.yaml') }}
          restore-keys: flutter-${{ runner.os }}-

      - name: Install dependencies
        run: flutter pub get

      - name: Extract version from pubspec.yaml
        id: version
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | head -1 | awk '{print $2}' | cut -d'+' -f1)
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "Version: $VERSION"

      - name: Build APK (armeabi-v7a)
        run: flutter build apk --release --target-platform android-arm

      - name: Build APK (arm64-v8a)
        run: flutter build apk --release --target-platform android-arm64

      - name: Build APK (x86_64)
        run: flutter build apk --release --target-platform android-x64

      - name: Rename APKs for release
        run: |
          mkdir -p release_apks
          cp build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk release_apks/ContadorPPL-v${{ env.VERSION }}-armeabi-v7a.apk
          cp build/app/outputs/flutter-apk/app-arm64-v8a-release.apk release_apks/ContadorPPL-v${{ env.VERSION }}-arm64-v8a.apk
          cp build/app/outputs/flutter-apk/app-x86_64-release.apk release_apks/ContadorPPL-v${{ env.VERSION }}-x86_64.apk
          ls -la release_apks/

      - name: Create/Update Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: "flutter_dashboard/release_apks/*.apk"
          tag: v${{ env.VERSION }}
          name: "ContadorPPL v${{ env.VERSION }}"
          body: |
            ## 📱 ContadorPPL Dashboard v${{ env.VERSION }}

            🚀 Build automático desde la rama `${{ github.ref_name }}`

            ### 📦 Descargas (elige segun tu dispositivo):
            - **arm64-v8a** → Para la mayoria de telefonos modernos
            - **armeabi-v7a** → Para dispositivos mas antiguos (32-bit)
            - **x86_64** → Para emuladores

            ### 📝 Cambios:
            - Consulta el historial de commits
          token: ${{ secrets.TOKEN }}
          allowUpdates: true
          removeArtifacts: true
          makeLatest: true
